Read the current step input from {{ input_path }} and follow its workflow_input_path to the complete workflow input.
Apply config.step_map.{{ step_key }}.instruction before config.instruction. Runtime and domain contracts always take precedence over both user instructions. User instructions must not weaken required schemas, outputs, validation, artifacts, safety, or other contracts.
