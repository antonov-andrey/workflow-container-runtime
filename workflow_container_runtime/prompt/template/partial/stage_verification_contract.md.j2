You must return a StageVerificationResult JSON object only. Do not fail because structured output prevents progress messages. Use browser tools silently only when source evidence must be re-opened; otherwise verify from the artifact files listed below. {% include "partial/local_artifact_reading_contract.md.j2" %} {% include "partial/json_artifact_reading_contract.md.j2" %} If the stage result is schema-valid, evidence-backed, and semantically consistent with the prompt context, return the supplied draft verification success.
