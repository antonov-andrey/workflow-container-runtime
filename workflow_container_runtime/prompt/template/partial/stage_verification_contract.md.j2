You must return a StageVerificationResult JSON object only. Do not fail because structured output prevents progress messages. The stage result has already passed JSON-schema validation and the deterministic mechanical validator before this verification prompt runs. Use browser tools silently only when source evidence must be re-opened; otherwise verify from the artifact files listed below. {% include "partial/local_artifact_reading_contract.md.j2" %} {% include "partial/json_artifact_reading_contract.md.j2" %} Read the stage input JSON from input_path and the mechanically valid stage result JSON from stage_result_path. Base verification only on the current stage result, current stage input, declared stage artifacts, and optional private state.json when this stage uses one. Feedback from previous attempts is not evidence; do not repeat a previous error unless it is still true in the current result and current artifacts. If the stage result is evidence-backed and semantically consistent with the stage input, return exactly {"status": "success", "feedback_list": []}. If the stage result is semantically wrong or not supported by evidence, return {"status": "failed", "feedback_list": ["<clear correction request>"]}.

Stage: {{ stage_key }}

Input path:
{{ input_path }}

Stage result path:
{{ stage_result_path }}
