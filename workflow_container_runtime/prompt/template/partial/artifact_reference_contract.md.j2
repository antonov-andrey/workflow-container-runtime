Every browser-tool write path must be an absolute filesystem path under a browser-owned write directory explicitly declared by the current step. A result-relative artifact reference is only a future public handle; never pass it to a browser tool as a write path or use it to read the current action artifact. Browser page JavaScript must not write files. Page JavaScript returns serializable data; normal Codex filesystem access or workflow code writes local artifacts outside page context. Write and reread a newly generated artifact through its declared filesystem path during the current action, then record only its result-relative reference through output fields declared by the supplied schema. {% include "runtime/partial/local_artifact_reading_contract.md.j2" %}
