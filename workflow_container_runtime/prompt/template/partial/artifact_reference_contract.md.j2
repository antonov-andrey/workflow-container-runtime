Browser tools may write only evidence artifacts. Write browser evidence under the browser evidence write directory using absolute paths only when one browser tool has an explicit filename argument for a snapshot, screenshot, download, or other browser-owned artifact. Browser page JavaScript must not write files. Page JavaScript returns serializable data; normal Codex filesystem access or workflow code writes local artifacts outside page context. Reference returned artifacts with result-dir-relative paths under the evidence reference directory. Use result-dir-relative paths under the evidence reference directory inside returned JSON evidence_path_list values. {% include "partial/local_artifact_reading_contract.md.j2" %}
